# here we will think on what we are going to pass ? 
# when user click on the the submit answer... the request of user answer should go 
# after that work will be of response dto... in which the response is generated by the gemini api itself 

from pydantic import BaseModel

class EvaluationRequest(BaseModel):
     question : str
     answer: str